import { Alert, ButtonLink, Card, PageHeader } from "@/components/ui"; import { prisma } from "@/lib/prisma"; import { createPerson } from "../actions"; import { PersonForm } from "../form";
export const dynamic = "force-dynamic"; type Params={result?:string;message?:string};
export default async function NewPerson({searchParams}:{searchParams:Promise<Params>}){const [params,locations]=await Promise.all([searchParams,prisma.location.findMany({where:{active:true},select:{id:true,name:true},orderBy:{name:"asc"}})]);return <><PageHeader title="Nova Pessoa" description="Cadastre um colaborador ou responsável." action={<ButtonLink href="/pessoas" variant="secondary">Voltar</ButtonLink>} /><Card>{params.message?<Alert status={params.result} message={params.message}/>:null}<PersonForm action={createPerson} locations={locations} submitLabel="Criar pessoa" /></Card></>}
