import { notFound } from "next/navigation"; import { Alert, ButtonLink, Card, PageHeader } from "@/components/ui"; import { prisma } from "@/lib/prisma"; import { updatePerson } from "../../actions"; import { PersonForm } from "../../form";
export const dynamic="force-dynamic"; type Params={result?:string;message?:string};
export default async function EditPerson({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<Params>}){const [{id},sp,locations,person]=await Promise.all([params,searchParams,prisma.location.findMany({where:{active:true},select:{id:true,name:true},orderBy:{name:"asc"}}),params.then(p=>prisma.person.findUnique({where:{id:p.id}}))]); if(!person) notFound(); return <><PageHeader title={`Editar ${person.name}`} description="Atualize os dados da pessoa." action={<ButtonLink href="/pessoas" variant="secondary">Voltar</ButtonLink>} /><Card>{sp.message?<Alert status={sp.result} message={sp.message}/>:null}<PersonForm action={updatePerson} person={person} locations={locations} submitLabel="Salvar pessoa" /></Card></>}
