import { Alert, ButtonLink, Card, PageHeader } from "@/components/ui"; import { prisma } from "@/lib/prisma"; import { createLocation } from "../actions"; import { LocationForm } from "../form";
export const dynamic="force-dynamic"; type Params={result?:string;message?:string};
export default async function NewLocation({searchParams}:{searchParams:Promise<Params>}){const [params,locations]=await Promise.all([searchParams,prisma.location.findMany({select:{id:true,name:true},orderBy:{name:"asc"}})]);return <><PageHeader title="Nova Localização" description="Cadastre uma base, loja ou área interna." action={<ButtonLink href="/localizacoes" variant="secondary">Voltar</ButtonLink>} /><Card>{params.message?<Alert status={params.result} message={params.message}/>:null}<LocationForm action={createLocation} locations={locations} submitLabel="Criar localização"/></Card></>}
